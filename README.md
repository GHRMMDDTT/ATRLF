# Another Tools Recognition Language Format

Welcome ATELF id a tooln for create a Lexer, using a similar format of **BNF**, with a **custom syntax**.

Note: is a combinatikn pf BNF (Backus-Naur Formalis) more PEG (Parsing Expression Grammatical).

> [Missing more text]

## How to compilate the project.

For _compilate_ the project **ATRLF**.

### 1.- Compilate the Java to Jar
Youbneed use this command for create a Java Runtime (JAR).

```sh
java -jar out/artifacts/ATRLF *.java
```

#### Move jar to ...


